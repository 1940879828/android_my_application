# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Daemon JVM: ~40GB RAM machine → give Gradle enough heap for Compose
org.gradle.jvmargs=-Xmx6g -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Speed: parallel task execution + reuse previous build outputs
org.gradle.parallel=true
org.gradle.caching=true

# Skip configuration phase when build scripts unchanged
org.gradle.configuration-cache=true

# Android / Kotlin
android.useAndroidX=true
kotlin.code.style=official
kotlin.incremental=true
